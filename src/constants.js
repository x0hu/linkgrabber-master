// Create this new file to share constants
export const DEFAULT_SETTINGS = {
  blockedDomains: ['bad1.example.com', 'bad2.example.com', 'bad4.example.com'],
  groupByDomain: true,
};

export const DEFAULT_SESSION = {
  tabData: {},
}; 